import 'package:flutter/material.dart';

class AppColors {
  // Dark Backgrounds (Spotify style)
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceVariant = Color(0xFF2A2A2A);

  // Accents (Neon Pink / Indigo)
  static const Color primary = Color(0xFFFF2D55); // Neon Pink
  static const Color secondary = Color(0xFF5E5CE6); // Indigo

  // Text
  static const Color onBackground = Colors.white;
  static const Color onSurface = Colors.white;
  static const Color onSurfaceVariant = Colors.white70;

  // Status
  static const Color error = Color(0xFFFF453A);
  static const Color success = Color(0xFF32D74B);
}
